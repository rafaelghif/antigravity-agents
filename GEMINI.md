# Workspace Bootstrap

Read `AGENTS.md` before acting. It is the single source of truth for always-on policy. Consult `.agents/brain/active_context.md` for active session state and `.agents/brain/memory.md` & `rules.md` for permanent project architecture, stack profile, and user preferences across all sessions.
