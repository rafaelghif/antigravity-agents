# Workspace Bootstrap

Read `AGENTS.md` before acting. It is single source of truth for L9 workflow.
- **Target**: Ship target project, not agent harness.
- **Anti-Hallucinate**: ALWAYS run `python3 scripts/grounding.py` and inspect files with `view_file` before coding. Never blind-overwrite.
- **Standup**: Run `python3 scripts/meeting_coordinator.py --standup` on feature tasks to sync `tasks/meeting_notes.md`.

