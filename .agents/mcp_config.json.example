{
  "mcpServers": {
    "github": {
      "serverURL": "https://api.githubcopilot.com/mcp/",
      "headers": {
        "Authorization": "Bearer ${GITHUB_PAT}"
      }
    },
    "gitea": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "-e",
        "GITEA_HOST=http://host.docker.internal:3000",
        "-e",
        "GITEA_ACCESS_TOKEN=${GITEA_PAT}",
        "gitea/gitea-mcp:latest"
      ]
    }
  }
}
