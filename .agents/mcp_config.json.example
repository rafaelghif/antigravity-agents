{
  "mcpServers": {
    "github": {
      "serverUrl": "https://api.githubcopilot.com/mcp/",
      "headers": {
        "Authorization": "Bearer ${GITHUB_PAT}"
      }
    },
    "gitea": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "-e",
        "GITEA_HOST=${GITEA_HOST}",
        "-e",
        "GITEA_ACCESS_TOKEN=${GITEA_TOKEN}",
        "gitea/gitea-mcp:v0.1.0"
      ]
    }
  }
}
